//-----------------------------------------------------------------
//Extension: Additional
//Extension template: Defines the structures for Extension Objetcts
//-----------------------------------------------------------------

// Defines the structure of custom element objects

// Class custom element extends element
/*
class debugElement extends Element{

    //Constuctor
    constructor(geometry){
        super(geometry);
    }

    //Non-chainable methods

    collision = () => null;

    getReferencePointOnPoint(point, tolerance){
        return null;
    }

    //Chainable methods
    rebuild = (cursorDocumentPosition) => {
        console.log("Rebuilding custom element");
        return this;
    }

    render = () => {
        background("#feda00");
    }

}

Commands.debugelement = function(document){
    return new Command(
    document, "debug",
    () => true,
    () => document.addElement(new debugElement()),
    () => null
    );
}
*/